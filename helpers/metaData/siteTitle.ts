import addressSeparator from 'AddressSeparator';
import siteName from 'SiteName';
import slogan from 'Slogan';

const siteTitle = `${siteName} ${addressSeparator} ${slogan}`;

export default siteTitle;

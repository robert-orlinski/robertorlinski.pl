import styled from 'styled-components';

export const Blockquote = styled.blockquote`
  padding-left: var(--text-gap);
  margin: calc(var(--text-gap) * 0.5) 0 calc(var(--text-gap) * 1.5) var(--text-gap);

  border-left: 4px solid #222;
`;
